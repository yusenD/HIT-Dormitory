<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.icephone.yuhao.repairerecord">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".view.RecordDetailActivity" />
        <activity android:name=".view.SearchRecordActivity" />
        <activity android:name=".view.CenterListActivity" />
        <activity android:name=".view.ResultActivity" />
        <activity android:name=".view.SuccessActivity" />
        <activity android:name=".view.ManagePeopleActivity" />
        <activity android:name=".view.ManageRepairProActivity" />
        <activity android:name=".view.ManageSiteActivity" />
        <activity android:name=".view.ManageFittingAvtivity" />
        <activity android:name=".view.InstallRecordDetailActivity" />
        <activity android:name=".view.PeopleDetailActivity" />
        <!-- <activity -->
        <!-- android:name=".LoginActivity" -->
        <!-- android:label="@string/title_activity_login"></activity> -->
        <activity android:name=".view.SearchInstallActivity" />
        <activity android:name=".view.ResultInstallActivity"></activity>
    </application>

</manifest>